{
  "rules": {
    "recitation": {
      "$classId": {
        ".read": true,
        ".write": "auth != null && auth.token.teacher === true"
      }
    },
    ".read": false,
    ".write": false
  }
}
