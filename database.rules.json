{
  "rules": {
    "recitation": {
      "$classId": {
        ".read": "$classId === '201' || $classId === '401'",
        ".write": "($classId === '201' || $classId === '401') && auth != null && auth.token.teacher === true"
      }
    },
    ".read": false,
    ".write": false
  }
}
